[project]
name = "companion-chat"
version = "0.1.0"
description = "Tiny cheeky companion chat — FastAPI + vanilla JS"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "httpx>=0.27",
    "python-dotenv>=1.0",
]
